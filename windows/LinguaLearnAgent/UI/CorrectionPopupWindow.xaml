<Window x:Class="LinguaLearnAgent.UI.CorrectionPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LinguaLearn Correction" Height="260" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" WindowStartupLocation="CenterScreen">
    <Border Background="#FFFFFFFF" BorderBrush="#FFCBD5E1" BorderThickness="1" CornerRadius="8" Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.25" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header Badge -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                <Border x:Name="BadgeBorder" Background="#FF22C55E" CornerRadius="4" Padding="6,2">
                    <TextBlock x:Name="BadgeText" Text="Grammar OK ✓" Foreground="White" FontWeight="Bold" FontSize="12"/>
                </Border>
                <TextBlock x:Name="SourceAppText" Text="LinguaLearn Agent" Foreground="#FF64748B" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </StackPanel>

            <!-- Russian Summary -->
            <TextBlock x:Name="SummaryTextBlock" Grid.Row="1" Text="Все ошибки исправлены." Foreground="#FF334155" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,8"/>

            <!-- Corrections & Errors List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
                <StackPanel x:Name="ContentPanel">
                    <TextBlock x:Name="OriginalTextBlock" Text="" Foreground="#FF64748B" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,4"/>
                    <TextBlock x:Name="CorrectedTextBlock" Text="" Foreground="#FF0F172A" FontWeight="SemiBold" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,4"/>
                    <ItemsControl x:Name="ErrorsListView">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FFF8FAFC" BorderBrush="#FFE2E8F0" BorderThickness="1" CornerRadius="4" Margin="0,2" Padding="6">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DetailsText}" Foreground="#FFDC2626" FontWeight="SemiBold" FontSize="12"/>
                                        <TextBlock Text="{Binding ExplanationRu}" Foreground="#FF475569" FontSize="11" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <!-- Action Buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="ReplaceButton" Content="Replace Text" Grid.Column="1" Width="100" Height="26" Margin="0,0,8,0" Click="ReplaceButton_Click"/>
                <Button x:Name="CloseButton" Content="Dismiss" Grid.Column="2" Width="70" Height="26" Click="CloseButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
